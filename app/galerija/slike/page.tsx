import React from 'react';
import NavWrapper from '@/app/components/NavWrapper';
import { NavEnum } from '@/app/components/Navigation';

const GalerijaSlike = () => {
  return (
    <NavWrapper selected={NavEnum.GALERIJA_SLIKE}>GalerijaSlike</NavWrapper>
  );
};

export default GalerijaSlike;
